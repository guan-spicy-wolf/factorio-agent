---
name: use-uv
description: User requires uv as the Python package/project manager, not pip or bare python
type: feedback
---

Use `uv` for all Python dependency and project management.

**Why:** User's explicit preference.
**How to apply:** Use `uv run` to execute scripts/tests, `uv add` for dependencies, `uv sync` for installs.
